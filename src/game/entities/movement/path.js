export default class Path {
  /**
   *
   * @param {import('../position').default} start
   * @param {import('../position').default} end
   */
  constructor (start, end) {
    this.start = start;
    this.end = end;
  }
}
