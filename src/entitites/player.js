export default class Player {
  /**
   *
   * @param {String} id
   * @param {Number} name
   * @param {String} color
   */
  constructor (id, name, color) {
    this.id = id;
    this.name = name;
    this.color = color;
  }
}
